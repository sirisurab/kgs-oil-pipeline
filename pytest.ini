[pytest]
testpaths = tests
markers =
    unit: Tests that do not require network access or real data files
    integration: Tests that require network access or real data files in data/
addopts = -v -m "not integration"
