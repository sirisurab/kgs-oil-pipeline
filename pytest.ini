[pytest]
markers =
    unit: fast unit tests (no network, no large files)
    integration: integration tests requiring real data on disk or network
testpaths = tests
addopts = -v -m "not integration"
