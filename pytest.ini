[pytest]
testpaths = tests
markers =
    unit: unit tests — require no network access or data files on disk
    integration: integration tests — may require data files on disk
addopts = -ra --tb=short
