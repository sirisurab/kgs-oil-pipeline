[pytest]
testpaths = tests
markers =
    unit: Does not require network access or data files on disk
    integration: Requires network access or real data files in data/
addopts = -v --tb=short -m "not integration"
asyncio_mode = auto
