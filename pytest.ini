[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    unit: unit tests — no network access or external data files required
    integration: integration tests — may require data files or network access
