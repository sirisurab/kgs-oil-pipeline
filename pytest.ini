[pytest]
testpaths = tests
markers =
    unit: Unit tests (fast, no network, no real data files)
    integration: Integration tests (require real data files or network)
addopts = -m "not integration"
